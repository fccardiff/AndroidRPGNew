package com.example.AndroidRPGNew.multiplayer;

import android.app.Activity;
import android.os.Bundle;
import android.util.Log;

import java.lang.Exception;
import java.sql.Connection;

public class SQLConnection extends Activity {

    public void onCreate(Bundle savedInstanceState){
        // Initiate new SQL connection, connect to database, save username + password (hashed?)
        // Use google's tutorial for connecting to SQL, rather than directly from the app
        super.onCreate(savedInstanceState);
    }


}